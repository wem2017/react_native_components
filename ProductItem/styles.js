import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  listContainer: {
    height: 140,
    borderRadius: 12,
    overflow: 'hidden',
    flexDirection: 'row',
    backgroundColor: 'red',
  },
});
